# Copy to .env (which is git-ignored) for local dev/CI scripts.
# Real keys go in the iPhone Keychain via Settings → AI provider in production.

# Anthropic Claude — primary provider
ANTHROPIC_API_KEY=

# Optional alternates
OPENAI_API_KEY=
XAI_API_KEY=
